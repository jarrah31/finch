# Production runtime dependencies — pinned for reproducible Docker builds.
# Versions are exact (==) so Docker images are always built with known-good packages.
# To upgrade: test locally first, update both this file and pyproject.toml, then release.
fastapi==0.135.1
uvicorn[standard]==0.42.0
aiosqlite==0.22.1
jinja2==3.1.6
python-multipart==0.0.22
itsdangerous==2.2.0
httpx==0.28.1
